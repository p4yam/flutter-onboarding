import 'package:flutter/material.dart';
import 'package:flutter_onboarding_animation/model/page_model.dart';

class SlidePage extends StatelessWidget {
  final PageModel pageModel;
  const SlidePage({Key key, this.pageModel}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
